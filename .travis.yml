git:
        submodule: false
notifications:
        emails: false

#before_install:
matrix:
        include:
                - os: linux
                  dist: bionic
                  language: node_js
                  node_js: 
                          - 10
                  script:
                          - npm install
                          - npm run coverage
 		          - bash <(curl -s https://codecov.io/bash) -f ./coverage/lcov.info;
                - os: windows
                  language: node_js
                  node_js: 
                          - 10
                  script:
                          - npm install
                          - npm run test
