// DeviceModel
export interface DeviceModel {
  serial: string;
  site_id: string;
  product: string;
  prj_version: string;
  atx_version: string;
  web_version: string;
  mac: string;
  last_seen: number;
}
